# git-cliff configuration for pulis-studio changelog format
# https://git-cliff.org/docs/configuration

[changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
"""
body = """
{% if version %}\
## [{{ version }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## Unreleased
{% endif %}

{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim }}

{% for commit in commits %}
- {{ commit.message | first_line }}{% if commit.author %} (@{{ commit.author.name | escape }}){% endif %}

{% endfor %}
{% endfor %}
"""
trim = true
footer = """
<!-- generated by git-cliff -->
"""
render_always = false

[git]
conventional_commits = true
filter_unconventional = true
tag_pattern = "^v(?P<major>\\d+)\\.(?P<minor>\\d+)\\.(?P<patch>\\d+)$"
commit_parsers = [
  { body = ".*BREAKING CHANGE.*", group = "Breaking Changes" },
  { message = "^feat", group = "Features" },
  { message = "^fix", group = "Bug Fixes" },
  { message = "^a11y", group = "Accessibility" },
  { message = "^doc", group = "Documentation" },
  { message = "^perf", group = "Performance" },
  { message = "^refactor", group = "Refactoring" },
  { message = "^revert", group = "Reverts" },
  { message = "^chore|^ci|^style|^test", group = "Chores" },
  { message = ".*", group = "Chores" },
]
protect_breaking_commits = true
sort_commits = "oldest"
